class UserList{
  String? name;
  String? image;
  String? lastSeen;
  bool? isActive;
  String? lastMessage;
  UserList({required this.name,required this.image,required this.lastSeen,required this.isActive,required this.lastMessage});
}